---
import Layout from '../../../layouts/Layout.astro';
import Navbar from '../../../components/astro/Navbar.astro';
import Footer from '../../../components/astro/Footer.astro';
import Breadcrumbs from '../../../components/astro/Breadcrumbs.astro';
import { type SupportedLanguage, languages } from '../../../types/language';
import { getDictionary } from '../../../i18n/get-dictionary';

export async function getStaticPaths() {
  const tools = [
    'headline-analyzer', 'lorem-generator', 'meta-generator', 'paa-scraper',
    'readability-checker', 'serp-preview', 'utm-builder', 'faq-generator',
    'cost-calculator', 'privacy-policy-generator', 'qr-generator',
    'social-media-preview', 'css-gradient-generator', 'glassmorphism-generator',
    'slug-generator'
  ];

  const params = [];
  for (const lang of Object.keys(languages)) {
    for (const tool of tools) {
      params.push({
        params: { lang, tool }
      });
    }
  }
  return params;
}

const { lang, tool } = Astro.params;
const supportedLang = lang as SupportedLanguage;
const dictionary = await getDictionary(supportedLang);

// Helper to translate strings
const t = (key: string, options?: { fallback: string }) => {
  const parts = key.split('.');
  let current: any = dictionary;
  for (const part of parts) {
    if (current && current[part] !== undefined) {
      current = current[part];
    } else {
      return options?.fallback || key;
    }
  }
  return typeof current === 'string' ? current : options?.fallback || key;
};

// Map slug to translation keys
const getToolMetadata = (toolVal: string) => {
  const formattedTitle = toolVal.replace(/-/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
  return {
    title: formattedTitle,
    desc: `Use our free online ${formattedTitle} tool to improve your web workflows and metadata tags.`
  };
};

const meta = getToolMetadata(tool);

const breadcrumbs = [
  { label: t('common.home'), url: `/${supportedLang}` },
  { label: t('tools.title', { fallback: 'Tools' }), url: `/${supportedLang}/tools` },
  { label: meta.title }
];
---

<Layout title={meta.title} description={meta.desc} lang={supportedLang} slug={`tools/${tool}`}>
  <Navbar lang={supportedLang} />

  <main class="grow w-full overflow-x-hidden bg-gray-50/50 pb-20 pt-20">
    <section class="bg-linear-to-b from-primary/5 to-white pt-32 pb-16 md:pt-40 md:pb-24">
      <div class="container mx-auto px-4 max-w-4xl text-center space-y-6">
          <Breadcrumbs items={breadcrumbs} lang={supportedLang} />

        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight font-persian">{meta.title}</h1>
        <p class="text-xl text-gray-600 font-persian leading-relaxed">{meta.desc}</p>
      </div>
    </section>

    <!-- Workspace card -->
    <section class="py-16 bg-white font-sans font-persian">
      <div class="container mx-auto px-4 max-w-3xl">
        <div class="bg-white border border-gray-100 rounded-2xl p-8 shadow-xs text-center space-y-6">
          <h2 class="text-2xl font-bold text-gray-900">{meta.title} Workspace</h2>
          <p class="text-gray-500 leading-relaxed max-w-lg mx-auto">
            This tool is currently undergoing automated refactoring to support faster client side calculations and offline processing.
          </p>
          <div class="pt-4">
            <a
              href={`/${supportedLang}/contact`}
              class="inline-block px-8 py-3 bg-primary text-white font-bold rounded-lg shadow-sm hover:bg-primary/95 transition-all"
            >
              Request Access
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer lang={supportedLang} />
</Layout>
